  <h4 class="title"> TÂCHE EN COURS </h4>

<div class="container">

  <div class="show_task_image">
    <p><%= image_tag @task.image, alt: 'task_image', class: 'task_image_large'%></p>
  </div>

  <p class="show_task_name"><%= @task.name %></p>
  <p class="show_task_description"><%= "\"#{@task.description}\"" %></p>

    <div class="submit_btn_show">

      <div class="submit_icon">
        <%= link_to icon('fas', 'check'), edit_user_task_path(@task.current_user_task),  class: "btn-check"%>
        <%= link_to image_tag('carotte_b_fo.png'), tasks_path, "data-toggle" => "modal", 'data-target' => '#exampleModal', class:"carot_show" %>
      </div>

      <div class="points">
        <p class="show_task_points"> + <%= @task.points %></p>
        <p class="show_task_points last"> - <%= @task.carotte_card_points %></p>
      </div>

    </div>

    <!-- <div class="submit_btn_show ">

      <p class="show_task_points"> + <%= @task.points %></p>
      <p class="show_task_points last"> - <%= @task.carotte_card_points %></p>

    </div> -->

    <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">Je carotte mon coloc !</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            Je carotte mon coloc !
          </div>
          <div class="modal-footer">
            <%= link_to "Oui!", carotte_user_task_path(@task.current_user_task), method: :post %>
          </div>
        </div>
      </div>
    </div>


</div>
