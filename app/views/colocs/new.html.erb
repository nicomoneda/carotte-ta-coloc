<%= simple_form_for(@coloc) do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name, placeholder: "Choose a name" %>
  </div>  
  <div>
      <%= f.submit class:'btn-submit' %>
  </div>
<% end %>